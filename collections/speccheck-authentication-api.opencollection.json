{
  "opencollection": "1.0.0",
  "info": {
    "name": "SpecCheck Authentication API",
    "version": "v1"
  },
  "request": {
    "auth": {
      "type": "bearer",
      "token": "{{bearerToken}}"
    }
  },
  "items": [
    {
      "info": {
        "name": "Authentication",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Create Access Token",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://api.speccheckrx.com/v1/oauth/token",
            "body": {
              "type": "json",
              "data": "{}"
            }
          },
          "docs": "Creates a bearer token for API authentication from your application's client credentials. The returned token expires after 24 hours; request a new token before each API session as a best practice."
        }
      ]
    }
  ],
  "bundled": true
}